FROM python:3.12-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml uv.lock README.md ./
COPY denview/ ./denview/
RUN uv sync --frozen --no-dev

EXPOSE 8000

CMD ["uv", "run", "denview", "serve", "--host", "0.0.0.0", "--port", "8000"]
